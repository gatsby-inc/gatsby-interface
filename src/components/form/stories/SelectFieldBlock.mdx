import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  SelectFieldBlock,
} from "../components/SelectFieldBlock"
import { FieldDocDisclaimer } from "./stories.utils"
import FieldSharedPropsDescription from './FieldSharedPropsDescription.mdx'

# SelectFieldBlock
----
<Preview>
<Story id="form-styled-blocks-selectfieldblock--basic" />
</Preview>

## Props
<Props of={SelectFieldBlock} />

`SelectFieldBlock` accepts these props:
### `options: { label: string; value: string }[]` (required)
An array of objects describing each option.

`SelectFieldBlock` also accepts all of the props supported by `<select />` as well as the props shared between field block components: 
<FieldSharedPropsDescription />
