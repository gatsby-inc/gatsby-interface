import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  RadioButtonField,
  RadioButtonFieldLabel,
  RadioButtonFieldOptions,
  RadioButtonFieldOptionItem,
  RadioButtonFieldHint,
  RadioButtonFieldError,
} from "../components/RadioButtonField"
import { FieldDocDisclaimer } from "./stories.utils"
import { getStoryOptions } from "../../../utils/storybook"

<Meta title="Form/Styled Primitives/RadioButtonField" />

# RadioButtonField
----
<FieldDocDisclaimer
  fieldType="radio button"
  blockComponentName="RadioButtonFieldBlock"
  connectedComponentName="RadioButtonConnectedField"
/>

<Preview>
  <Story name="Basic">
    <RadioButtonField id="basic" hasError hasHint>
      <RadioButtonFieldLabel isRequired>Field label</RadioButtonFieldLabel>
      <RadioButtonFieldOptions>
        {getStoryOptions('short').map(({ value, label }, idx) => (
          <RadioButtonFieldOptionItem
            key={value}
            value={value}
            label={label}
            name="basic"
            defaultChecked={idx === 0}
          />
        ))}
      </RadioButtonFieldOptions>
      <RadioButtonFieldHint>Field hint</RadioButtonFieldHint>
      <RadioButtonFieldError>Field error</RadioButtonFieldError>
    </RadioButtonField>
  </Story>
</Preview>

<Props components={{ RadioButtonFieldLabel, RadioButtonFieldOptions, RadioButtonFieldOptionItem, RadioButtonFieldHint, RadioButtonFieldError }} />
