import { AtomTone } from "../../theme/types"

export type BadgeVariant = `PILL` | `STATUS`
export type BadgeTextVariant = `DEFAULT` | `EMPHASIZED` | `CAPS`
export type BadgeSize = `M` | `S` | `XS` | `XXS`
export type BadgeShape = `DEFAULT` | `PILL`
export type BadgeTone = AtomTone
