import { AtomTone } from "../../theme/types"

export type BadgeVariant = `PILL` | `STATUS`
export type BadgeText = `DEFAULT` | `CAPS`
export type BadgeSize = `MEDIUM` | `SMALL`
export type BadgeTone = AtomTone
